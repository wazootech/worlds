---
title: "First Steps"
description:
  "Initial actions after installation and how to use AI coding agents with
  Worlds."
---

# First Steps with Worlds

Once you have installed the Worlds SDK, the next step is to begin building your
stateful world models. We recommend starting with **Vibe Coding** to let AI
agents handle the heavy lifting of knowledge integration.

## Vibe Coding

["Vibe Coding"](https://x.com/karpathy/status/1886192184808149383?lang=en) is
the practice of using AI agents (like Claude Code, Windsurf, or Cursor) to build
applications through high-level intent rather than manual line-by-line coding.

Worlds is uniquely suited for Vibe Coding because its state is semantic and
malleable. You can treat the AI output as a completed unit while maintaining a
persistent, structured memory.

### The Agent Prompt

To help an AI agent integrate with Worlds, copy and paste the following
"World-Aware" prompt into your agent's context:

```markdown
I am building an application using Worlds. Worlds are persistent, semantic
knowledge containers.

- Use the `@wazoo/worlds-sdk` SDK (available on JSR) to interact with them.
- Every World has a `worldId`.
- You can store data as "Malleable Knowledge" which is automatically indexed and
  searchable.
- Use the `search` endpoint to retrieve context from a World.

When I ask you to "Remember X", call the Worlds SDK to store it. When I ask a
question about X, search the World for context first.
```

---

## Agent Integration

If you are using **Claude Code**, you can add Worlds as a custom skill by
providing it the OpenAPI spec:

1.  **Get your API Key**: From the [Worlds Console](https://console.wazoo.dev).
2.  **Mount the Spec**: Tell Claude to read
    `https://docs.wazoo.dev/openapi.json`.
3.  **Command**:
    `claude "Help me build a personal memory world for my project."`

## Best Practices

- **Semantic Naming**: Give your Worlds descriptive names. The AI uses these
  names to choose the right container for context.
- **Verbose Connectors**: When setting up connectors, let the AI agent handle
  the configuration logic.
- **Context Loops**: Always ask the agent to "check the world" at the start of a
  session.

## AI Discovery

Standard indices help LLM-based agents understand your documentation. Use these
in agent prompts or context:

- **[llms.txt](/llms.txt)** — Concise index for quick agent context.
- **[llms-full.txt](/llms-full.txt)** — Full documentation index for deep
  discovery.
